<form method="post" action="{{ path('randonnee_delete', {'id': randonnee.id}) }}" onsubmit="return confirm('Confirmer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ randonnee.id) }}">
    <button class="btn">Supprimer</button>
</form>
